<h1 class="page-header">Upcoming Events</h1>

<% @events.each do |event| %>

<p><%= link_to "#{event.name}", event_path(event) %> | Date/Time: <%= event.time.to_s(:time_display) %> | capacity: <%= event.capacity %> | Type: <%= event.eventcats.first.name if event.eventcats.any? %></p> 

<% end %>
